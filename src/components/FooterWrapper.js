import styled from 'styled-components';

const FooterWrapper = styled.div`
  color: green;
  font-style: italic;
  font-size: 20;
  text-align: center;
  margin: 0 auto;
`;

export default FooterWrapper;
